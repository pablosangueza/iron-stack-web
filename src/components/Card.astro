---
interface Props {
  title: string;
  body?: string;
}

const { title, body } = Astro.props;
---

<article class="card fade-up">
  <h3 class="text-lg font-semibold text-white">{title}</h3>
  {body && <p class="mt-3 text-sm leading-relaxed">{body}</p>}
  <slot />
</article>
